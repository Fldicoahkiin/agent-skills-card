# 本地 dev 用:复制为 .dev.vars 后按需填一个「只读、无 scope」的 GitHub PAT。
#   cp .dev.vars.example .dev.vars
# 不填走匿名 60/h(按出口 IP,共享,易被耗光);填了 5000/h(按账号)。
# .dev.vars 已 gitignore,不会提交。生产环境用 `wrangler secret put GITHUB_TOKEN`。
GITHUB_TOKEN=
